<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClickingGame.Resources.Styles.MyStyles">
    
    <Style TargetType="Entry" x:Key="LoginStyle">
        <Setter Property="TextColor" Value="Red"/>
        <Setter Property="FontSize" Value="Large"/>
        <Setter Property="Margin" Value="0,0,0,20"/>
    </Style>
    <Style TargetType="Image" x:Key="GameIconStyle">
        <Setter Property="VerticalOptions" Value="Center"/>
        <Setter Property="WidthRequest" Value="100"/>
        <Setter Property="HeightRequest" Value="100"/>
        <Setter Property="HorizontalOptions" Value="Center"/>
        <Setter Property="BackgroundColor" Value="{OnPlatform Android='Green', iOS='Silver', WinUI='Blue', Default='Gray'}"/>
    </Style>
    <Style TargetType="BoxView" x:Key="DetailViewBox">
        <Setter Property="Grid.Column" Value="0" />
        <Setter Property="Grid.Row" Value="0" />
        <Setter Property="Grid.RowSpan" Value="3" />
        <Setter Property="HorizontalOptions" Value="FillAndExpand"/>
        <Setter Property="VerticalOptions" Value="FillAndExpand"/>
        <Setter Property="CornerRadius" Value="20" />
    </Style>
</ResourceDictionary>